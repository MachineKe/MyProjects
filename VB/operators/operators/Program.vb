Imports System

Module Program
    Sub Main(args As String())
        Dim x As Integer = 5
        Dim y As Integer = 10
        Dim sum As Integer = x + y
        Console.WriteLine("The sum is " & sum)
        Console.ReadLine()
    End Sub
End Module
